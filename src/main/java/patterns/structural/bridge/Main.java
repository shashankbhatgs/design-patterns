package patterns.structural.bridge;

// Separating the unseparable (separates abstraction(Shape) from its implementation(rendering method))
// so that two can evolve independently
// what is being done (shapes)
// how it is being done (rendering methods) Provide specific implementation for rendering methods

public class Main {
}

